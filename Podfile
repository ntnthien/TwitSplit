# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TwitSplit' do
  # Pods for TwitSplit
  use_frameworks!
  pod 'RxSwift',    '~> 4.0'
  pod 'RxCocoa',    '~> 4.0'

  target 'TwitSplitTests' do
    inherit! :search_paths
    pod 'RxBlocking', '~> 4.0'
    pod 'RxTest',     '~> 4.0'
    # Pods for testing
  end

  target 'TwitSplitUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
